

const getRecommendedStoriesService = async(profile_id)=>{
    try {
        //first we find the profile
        // second we stories who's isnt read by that user
        // third remove the stories type user hates
        // fourth then get the score of each story and sort the stories by scores
        // fifth get the stories 
    } catch (error) {
        return Promise.reject(error);
    }
}